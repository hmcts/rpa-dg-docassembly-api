spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/rpa
      aliases:
        microservicekey-dg-docassembly-api: S2S_KEY
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        docmosis-access-key: DOCMOSIS_ACCESS_KEY
        docmosis-templates-auth: DOCMOSIS_TEMPLATES_ENDPOINT_AUTH

